# Repository-specific identifiers, quotations and deliberate fixtures.

schema = 1

[oxford]
stems = []

[words]
accepted = ["analyzer", "mold"]

[words.corrections]

[patterns]
ignore = [
  'Center \| Microsoft Learn',
  # Inline code spans were masked by the previous dictionary snapshot;
  # kept until the shared dictionary rules on it.
  '`[^`\n]+`',
]

[files]
exclude = []
